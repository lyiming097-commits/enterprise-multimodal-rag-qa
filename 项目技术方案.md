# 个人多模态 RAG 知识库 Demo 技术方案

## 1. 项目范围

实现一个可在个人电脑上运行的知识库问答 Demo，支持 PDF、Markdown、TXT 和图片。前端使用 Vue 3，后端使用 FastAPI。在线问答严格采用“Query 预处理、Query Embedding、多路召回、Cross-Encoder 精排、Prompt 拼装、生成与溯源”六步流程。

Demo 明确不包含 Docker、Redis、消息队列、持久化缓存、复杂权限和分布式部署。文件上传后同步建立索引，BM25 在查询时从当前有效 Chunk 构建。

## 2. 技术栈

| 能力 | 选型 | 运行方式 |
| --- | --- | --- |
| 界面 | Streamlit | 本地单进程 |
| LLM | DeepSeek `deepseek-chat` | 远程 API |
| RAG 组件 | LangChain Core / LangChain OpenAI | Python 进程内 |
| 数据库 | PostgreSQL + pgvector | 本机服务 |
| Embedding | Qwen3-Embedding-0.6B | 本机 Ollama |
| 稀疏检索 | 自实现中文 BM25 | 查询时进程内构建 |
| 融合 | Reciprocal Rank Fusion | Python 进程内 |
| Rerank | BAAI/bge-reranker-v2-m3 | 本地 Cross-Encoder |
| PDF | PyMuPDF | Python 进程内 |
| OCR | PaddleOCR 优先 + RapidOCR ONNXRuntime 回退 | 本地加载，扫描件必经 |
| VLM | Moondream（本地测试默认） | Ollama，本地图片和版面增强；可切换 Qwen2.5-VL |
| 数据访问 | SQLAlchemy + psycopg | Python 进程内 |
| 评测 | Hit@K、MRR、nDCG，预留 RAGAS | 本地脚本 |

## 3. 系统流程

```text
Streamlit 上传文件
    ↓
文件 SHA-256 去重和版本判断
    ↓
PDF / Markdown / TXT / 图片解析
    ↓
OCR；低文字量或低置信图片可选 VLM 描述
    ↓
清洗、结构感知切块、绑定页码与章节
    ↓
Ollama Qwen3 Embedding
    ↓
PostgreSQL + pgvector

用户提问
    ↓
① DeepSeek 改写 + HyDE + 2～3 个互补角度
    ↓
② Qwen3 Query Embedding；HyDE 使用 Document Embedding
    ↓
③ pgvector/BM25 多路召回 + RRF 融合
    ↓
④ bge-reranker-v2-m3 Cross-Encoder 精排
    ↓
⑤ 拼装带来源、受规则约束的 Prompt
    ↓
⑥ DeepSeek 生成 [S1] 引用，程序核验并展示来源
```

## 4. 核心配置

```dotenv
DATABASE_URL=postgresql+psycopg://localhost:5432/rag

DEEPSEEK_API_KEY=replace-me
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_EMBED_MODEL=qwen3-embedding:0.6b
EMBEDDING_DIMENSION=1024

RERANK_ENABLED=true
RERANK_MODEL=BAAI/bge-reranker-v2-m3
RERANK_BATCH_SIZE=8

OCR_ENABLED=true
OCR_BACKEND=auto
OCR_MIN_TEXT_CHARS=30
OCR_MIN_CONFIDENCE=0.75
VLM_ENABLED=true
OLLAMA_VLM_MODEL=moondream:latest
VLM_ON_IMAGES=true
VLM_ON_PDF_PAGES_WITH_IMAGES=true
```

本机已确认 Ollama 标签为 `qwen3-embedding:0.6b`，实际返回 1024 维向量。程序仍会检查每个返回向量的维度，避免与 pgvector 字段不一致。

## 5. 文档处理

### PDF

- PyMuPDF 按页面与文字块提取内容；
- 保存页码和文字区域坐标；
- 页面可提取文本少于阈值或检测到嵌入图像时转成图片执行 OCR；
- 图片、图表和含图 PDF 页面可执行 VLM 视觉描述；
- OCR 置信度低于阈值或文字量不足时自动追加 VLM；
- 每个 Chunk 记录 OCR 后端、置信度、坐标、页面和 VLM 模型来源。

### Markdown 和 TXT

- Markdown 按标题层级拆分；
- 保留标题路径，引用时展示章节；
- TXT 作为普通文本处理。

### 切块

- 初始大小 700 字符，重叠 100 字符；
- 优先按中文标点和换行切分；
- 每个 Chunk 保存文档、版本、序号、页码、章节、内容 Hash 和解析元数据；
- 估算 Token 数用于后续上下文预算。

## 6. 增量更新

- 首次上传：创建文档与版本，解析并写入有效 Chunk；
- 同名同 Hash：直接跳过，不重复向量化；
- 同名不同 Hash：创建新版本，新索引成功后停用旧 Chunk；
- 索引失败：旧版本继续有效，新版本记录失败原因；
- 删除：文档标记为删除，相关 Chunk 立即退出在线检索。

这种设计能在不引入任务队列的情况下演示文档新增、修改和删除的一致性。

## 7. 六步在线检索与生成

### 第一步：Query 预处理

- 始终保留原始问题；
- DeepSeek 生成一个消除口语和指代的改写查询；
- 生成 2～3 个覆盖术语、原因、结果或比较关系的互补查询；
- 生成一段简短的 HyDE 假设文档，仅用于检索，不作为答案或事实来源；
- 模型返回异常时退回原始问题，保证检索链路仍可运行。

### 第二步：Query Embedding

- 原问题、改写问题和扩写问题使用 Qwen3 Query 指令模板；
- HyDE 模拟知识库段落，因此使用 Document Embedding 方式；
- Query 与建库均调用同一个 Ollama 模型 `qwen3-embedding:0.6b`；
- pgvector 查询额外过滤 `embedding_model`，避免混用旧模型生成的向量。

### 第三步：多路召回与 RRF

- 使用 cosine distance；
- 建立 HNSW 索引；
- 每个普通查询各自执行向量召回；
- HyDE 单独执行一路向量召回；
- 每个普通查询各自执行一路 BM25；
- 每路默认召回 20 个 Chunk，随后使用 RRF 融合排名。

BM25 分词策略：

- 英文和数字按词切分；
- 中文同时使用单字与双字切分；
- 每次查询从数据库加载当前知识库的有效 Chunk；
- 默认召回 20 个 Chunk。

### 第四步：Cross-Encoder Rerank

- 取前 30 个融合结果；
- 使用 `BAAI/bge-reranker-v2-m3` 对原始 Query 与候选 Chunk 联合编码并打分；
- 批量精排到前 8 个；
- 最多向 DeepSeek 提供 6 个上下文 Chunk。

### 第五步：Prompt 拼装

- Chunk 使用 `[S1]` 等稳定编号；
- 写入文档名、页码、章节和原文；
- 明确规定资料是证据而不是指令，防止文档内 Prompt Injection；
- 要求只根据资料回答，不足时输出“根据当前知识库无法确定”。

### 第六步：生成与溯源

- DeepSeek 生成答案并在关键事实后标注证据编号；
- 程序拒绝本轮上下文中不存在的引用编号；
- 页面展示引用对应的文档、页码、章节及原文摘要；
- 调试页展示预处理结果、每条召回路径、RRF 分数和 Cross-Encoder 分数。

## 8. 引用与可信回答

上下文使用 `[S1]`、`[S2]` 编号。Prompt 要求关键事实引用证据；生成后程序只接受本次上下文中真实存在的编号，并展示：

- 原文档名；
- PDF 页码；
- Markdown 章节；
- 对应 Chunk 的原文摘要。

当没有检索结果时，不向模型提供虚构背景，并要求回答“根据当前知识库无法确定”。

## 9. 数据模型

- `knowledge_bases`：知识库名称和索引版本；
- `documents`：文件名、路径、SHA-256、当前版本和状态；
- `document_versions`：每次导入的版本、切块配置和失败信息；
- `chunks`：正文、页码、章节、JSON 元数据、Embedding 与有效状态。

## 10. 页面功能

### 文档管理

- 创建和切换知识库；
- 批量上传文件；
- 同步显示解析与索引进度；
- 查看状态和版本；
- 删除文档。

### 知识问答

- 保留当前页面会话历史；
- 使用 DeepSeek 回答；
- 展示有效引用。

### 检索信息

- 分六步展示 Query 改写、HyDE、多角度扩写和 Embedding 模型；
- 展示各 Chunk 的向量/BM25 命中路径、RRF 分数与 Rerank 分数；
- 展示问题路由、Prompt 约束、引用 Chunk 和来源；
- 不写入缓存或持久化会话。

## 11. 评测

内置 JSONL 检索评测脚本，数据格式为：

```json
{"question":"问题","relevant_documents":["正确来源.pdf"]}
```

输出 Hit@K、MRR 和 nDCG@K。后续需要进行完整生成评测时，安装可选 `eval` 依赖并接入 RAGAS，评估 Faithfulness、Answer Relevancy、Context Precision 和 Context Recall。

## 12. 本地运行

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
cp .env.example .env
python -m scripts.init_db
streamlit run web/streamlit_app.py
```

图片和扫描件支持：

```bash
pip install -e ".[ocr]"
```

## 13. Demo 验收标准

1. 能创建知识库并上传 Markdown、文本型 PDF；
2. 安装 OCR 后能提取图片和扫描 PDF；
3. 文档被切块并写入 pgvector，来源信息完整；
4. 同名同内容文件不会重复索引；
5. 修改或删除文档后不会召回旧 Chunk；
6. 问答同时使用 BM25 与向量召回；
7. 回答引用能映射到真实文档、页码或章节；
8. 可以通过脚本输出检索评测指标；
9. 整个 Demo 不依赖 Docker、Redis、消息队列或缓存。
